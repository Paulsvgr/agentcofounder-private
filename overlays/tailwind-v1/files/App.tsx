export function App() {
  return (
    <main className="min-h-screen bg-slate-50 p-8 text-slate-900">
      <section
        className="mx-auto max-w-xl rounded-xl border border-slate-200 bg-white p-8 shadow-sm"
        aria-labelledby="starter-title"
      >
        <p className="mb-2 text-xs font-bold uppercase tracking-wider text-blue-600">
          AgentCofounder
        </p>
        <h1 id="starter-title" className="mb-3 text-3xl font-semibold tracking-tight">
          Ready to build
        </h1>
        <p className="m-0 text-slate-600">
          Tailwind CSS is preinstalled. Compose the UI with utilities; replace this seed during the
          challenge.
        </p>
      </section>
    </main>
  );
}
