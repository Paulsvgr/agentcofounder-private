# Resource registry (repo root)

Canonical payloads and JSON registry entries for V2 resource assembly.

```text
registry/     JSON metadata (source of truth)
files/        File trees assembler copies into app-template
docs/         full_docs_reference targets (lazy retrieval)
```

Human documentation: [docs/v2/resources/README.md](../docs/v2/resources/README.md)

**Status:** data-pattern `local-storage-collection` is implemented (Exp5b). shadcn component entries are stubs pending research/import.
