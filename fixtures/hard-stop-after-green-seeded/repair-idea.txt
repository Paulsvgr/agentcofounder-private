The Home Library / bookshelf app already exists in this workspace (source and tests are present).

Do not rebuild from scratch or replace the whole application.

1. Call `verify` first.
2. When verification is green, write `report.partial.json` and finish.
3. Prefer fixing real failures if verify is not green; avoid unrelated rewrites.
