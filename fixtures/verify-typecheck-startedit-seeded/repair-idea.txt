The Home Library / bookshelf app already exists in this workspace (source and tests are present).

Do not rebuild from scratch or replace the whole application.

1. Call `verify` first.
2. Repair until `verify` is green.
3. Prefer fixing the real product or test bug that explains the failure; avoid unrelated rewrites.
4. When green, write `report.partial.json` and finish.

Product context: bookshelf with add + inline edit.
